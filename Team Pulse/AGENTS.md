# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

# TeamPulse Hub — макет

Контекст области — в `../AGENTS.md`, контекст workspace — в `../../AGENTS.md`.
`CLAUDE.md` и `NESSY.md` рядом — симлинки на этот файл, правь только `AGENTS.md`.

## Что это

Самостоятельный HTML-прототип верхнеуровневого HR-борта для руководителей.
**Библиотек нет вообще** — ни ECharts, ни любых других: конечный инструмент их не
поддерживает, поэтому всё рисуется на голом SVG в `draw.js`. **Это не график Proteus** —
контракт пяти файлов (`.chart.js` / `.data.sql` / `FIELDS.md` / `SELF_CHECK.md`),
ES5-ограничения и `validate.py` из `../../Графики Echarts/AGENTS.md` здесь
**не действуют**. Тут обычный современный JS (`const`, стрелки, шаблонные строки)
и 15 `<script>` в `index.html`.

Заказчику сдаётся **одним файлом**: `node build.js` собирает `index.standalone.html`
(~118 КБ) со встроенными стилями и скриптами, без единой внешней ссылки.

Правило области «макет только для чтения» для этой папки тоже снято: макет здесь правится.
Папка не под git, отката нет — правки делать точечными, файл целиком не переписывать.

## Запуск и проверка

```bash
python3 -m http.server 8765          # из этой папки, открыть http://localhost:8765
node --check draw.js                 # синтаксис, после каждой правки JS
node smoke.js                        # 13 экранов + 45 проверок правил, без браузера
node build.js                        # сборка index.standalone.html для сдачи
```

`smoke.js` прогоняет `render()` по каждой вкладке и под-вкладке (список под-вкладок берёт
из самих экранов, поэтому разойтись с кодом не может) и печатает размер собранного HTML,
число графиков и таблиц (считаются `.svgchart`, а не теги `<svg>`: иконки интерфейса
тоже рисуются в SVG). Дальше — 45 проверок правил, которые нельзя нарушать: ось от нуля,
отсутствие оси Y, обнуление накопительной текучести в январе, формула текучести, разбивки
таблицами, геометрия дивергентного чарта, отсутствие жёлтого в светофоре, привязка высоты
рабочей зоны к экрану, состав под-вкладок каждого блока, сходимость календаря офиса
с метрикой и весь механизм выбора метрик (скрытое не попадает в строки и столбцы,
обязательные метрики не скрываются, пустой блок уходит из навигации).
Хотя бы один FAIL — работа не сдана. Визуальную приёмку тест не заменяет.

Рисование целиком строковое: `draw.js` возвращает разметку SVG, а не работает с DOM.
Поэтому smoke гоняет **настоящий** рисовальный код без браузера и без заглушек.

Файлы держим в **NFC**: изначально они были в macOS-NFD (й = и + комбинирующая бреве),
из-за чего точечный поиск по строкам с «й» не находил совпадений. Если правки внезапно
перестали применяться — проверить нормализацию.

Смоук-тест данных без браузера (`data.js` обращается к `window` только в последней строке):

```bash
node -e "global.window={};require('./data.js');const D=window.TPDATA;
const st=D.DEFAULT_STATE;console.log(D.reportLeaves(st).length,D.benchmarkLeaves(st).length,D.benchmarkLabel(st))"
```

Сверено 31.07.2026: 370 узлов / 204 листа, при `DEFAULT_STATE` (unit `T/01`, paint HQ) —
7 команд в отборе против 79 в базе, hc_total 191, месячная текучесть 0,5–3,1% при базе
1,0–2,0%, годовой прогон ~21%, regret 4,7% при KPI 4/7.

Интернет нужен только для шрифта Inter из CDN. В `index.standalone.html` эта ссылка
вырезается, шрифт деградирует до Helvetica/Arial — это заложено в стек `font-family`.

## Архитектура

Разбивка **по слоям, потом по экранам**. Порядок загрузки жёсткий, каждый следующий файл
читает неймспейс предыдущего:

`data.js` → `draw.js` → `ui.js` → `insights.js` → `screens/_block.js` →
`screens/<блоки>.js` → `screens/onepager.js` → `app.js`

| Файл | Неймспейс | Ответственность |
|---|---|---|
| `data.js` | `TPDATA` | Оргдерево, метрики, генератор моков, агрегация, формулы текучести, форматирование, светофор |
| `draw.js` | `TPDRAW` | Рисование на голом SVG: line, bars, diverge, panels, waterfall, funnel, calendar + 2 спарклайна |
| `ui.js` | `TPUI` | Общие элементы: KPI-карточка, чипы, ячейка сравнения, плашка инсайта, `barTable` |
| `insights.js` | `TPINSIGHTS` | Отбор фактов по порогам `INS_GAP` / `INS_TREND` / `INS_CONC` |
| `screens/_block.js` | `TPSCREENS` | Каркас детальной вкладки + реестр экранов + общие помощники |
| `screens/<блок>.js` | — | Только специфика блока: под-вкладки, заголовки, содержимое правой панели |
| `screens/onepager.js` | — | Сводка по всем метрикам |
| `app.js` | — | State, URL, шапка, навигация, модалка, роутер, события |
| `styles.css` | — | Токены и компоненты; вёрстка собирается строками в экранах |
| `build.js` | — | Сборка всего в один `index.standalone.html` |

### Правило, которое держит разбивку

**Файл экрана не пишет разметку сам.** Ни строк SVG, ни `<table>`. Только сбор данных и
вызовы `TPDRAW` / `TPUI`. Нужен новый визуальный элемент — он сначала появляется в
`draw.js` или `ui.js`, и только потом используется экраном.

Иначе переиспользуемые элементы расходятся между экранами — ровно то, чего разбивка по
файлам должна избегать. Смысл разбивки в том, что «поправь экран текучести» читает
`screens/turnover.js` на 2 КБ, а не `app.js` на 40 КБ.

### Как графики попадают на экран

`TPDRAW.chart()` сразу рисует SVG при номинальной ширине 900px и складывает спеку в
реестр. После вставки в DOM `app.js` вызывает `TPDRAW.remeasure()`, и SVG
перерисовывается под фактическую ширину контейнера. Целиком масштабировать SVG нельзя —
на телефоне подписи стали бы нечитаемыми, поэтому кегль всегда 10–11px, а меняется
геометрия. Реестр чистится `TPDRAW.reset()` в начале каждого рендера.

Данные генерируются на лету из seed'а (`mulberry32` + FNV-хеш пути узла) — при одинаковом
пути значения всегда одни и те же, поэтому скриншоты воспроизводимы. На проде эти структуры
приходят из хранилища.

Агрегация: метрики из `COUNT_METRICS` суммируются, остальные — взвешенное среднее по
`hc_total`. Результат кешируется в `Map` по ключу «метрика + хеш списка листьев».

## Главное правило (ломать нельзя)

База сравнения выводится **из фильтров, а не из подразделения**:

```
reportLeaves(st)    = листья под st.drillRoot||st.unit, прошедшие paint+itSeg+staffType
benchmarkLeaves(st) = ВСЕ листья компании, прошедшие paint+itSeg+staffType
```

Подпись собирается автоматически (`benchmarkLabel`): «вся компания» / «всё HQ» /
«всё HQ IT штат». Drill по кнопке `↓` меняет `drillRoot`, но базу **не трогает** — про это
явно сказано пользователю в `note-inline`.

Исключение: `regret` сравнивается с KPI `{green:4,red:7}`, но только при `paint==='HQ'`
(`kpiFor`). В остальных случаях — со средней по базе.

Светофор `compareState`: мёртвая зона ±5%, `better:'flat'` (численность, найм, переводы)
всегда `neutral` — «больше» здесь не значит «лучше», это отдельно объяснено в легенде
и в модалке «Как читать отчёт».

**Изменение показывается знаком, а не стрелкой** (`TPUI.deltaChip`, действует на KPI-карточки
и OnePager разом — обе рисуются через него). `+7`, `−4`, при нуле просто `0` без значка.
Стрелка и цвет кодировали один факт дважды, и их приходилось сверять между собой:
`↓` у текучести — хорошо, `↓` у найма — никак. Разделение жёсткое: **знак отвечает за
направление, класс `up`/`down`/`neu` — за оценку**. Поэтому у `turnover_m` рост `+1,4 п.п.`
получает класс `down` (красный), а падение — `up`; у метрик с `better:'flat'` любой знак
даёт `neu`. Возвращать стрелки нельзя: они снова начнут спорить с цветом.

**В светофоре два цвета: зелёный и красный. Жёлтого нет.** Состояние `warn`
(«на уровне ±5%» у сравнения с базой, «между целью и порогом» у KPI) в логике осталось —
от него зависят подписи пилюль «на уровне» и «зона риска», — но красится оно серым,
тем же, что `neutral`. Причина от заказчика: жёлтая пилюля рядом с жёлтым баром
заставляла присматриваться туда, где повода вмешиваться нет. Держится в трёх местах:
`stateColor()` в `draw.js` (warn → `C_FLAT`), классы `.sig-chip.warn` / `.cell.warn` /
`.m-name.bar-warn` в `styles.css`, легенда `TPUI.trafficLegend` и справка в `index.html`
(три пункта вместо четырёх: серый закрывает и «на уровне», и «без оценки»).
Отдельного `C_WARN` в палитре больше нет — если он снова понадобится, это возврат
запрещённого третьего сигнала, а не мелкая правка.

Жёлтый остался только там, где он не про отклонение метрики: `.ai-sev.mid` / `.ai.sev-mid`
(severity плашки инсайтов, шкала high/mid/good) и `.pd-dot.warn` (настроение Пульса).
Это другой канал — не путать со светофором.

## Второе правило: сравнимость метрик

`D.comparable(key)` — с базой сравниваются только относительные метрики (8 из 18):
`turnover_m`, `turnover_y`, `regret`, `time_to_fill`, `tgrowth_conv`, `low_perf`,
`underwork`, `office_att`. Все счётные из `COUNT_METRICS` — никогда: 212 человек против
2 968 по компании это масштаб, а не оценка; то же с наймом, вакансиями и T-ростом в штуках.

Следствия в UI: вместо базы — пометка «не сравнивается» (`noCmpMark()`), линия базы
на графике не рисуется, колонка «К базе» появляется только если сравнима основная метрика
блока, инсайты про разрыв с базой и концентрацию отклонения считаются только по сравнимым
(тренд — по всем). Основная метрика таблицы — первая сравнимая в блоке.

## Третье правило: состав метрик выбирает пользователь

В state лежит `hiddenMetrics` — **скрытые** метрики, а не показанные. Инверсия
намеренная: пустой список означает «показать всё», и новая метрика в `METRICS`
появляется у всех сама, вместо того чтобы потеряться у тех, кто уже настроил дашборд.
Набор едет в ссылке параметром `hide=` и нормализуется при чтении (`sanitizeHidden`
выбрасывает неизвестные ключи и попытку скрыть обязательную метрику).

`LOCKED_METRICS` = `hc_active` + `hc_total` — выключить нельзя. Активная численность
опора отчёта, общая — знаменатель долей и подпись «N чел» в каждой строке сводной
таблицы. Побочно это гарантирует, что блок `structure` никогда не пустой.

Хелперы в `TPDATA`: `metricVisible(key,S)`, `visibleMetricsOfBlock(bk,S)`,
`visibleBlocks(S)`, `blockVisible(bk,S)`, `visibleCount(S)`, плюс `METRIC_PRESETS`
с `hiddenForPreset(key)` / `activePreset(S)` для четырёх быстрых наборов
(Всё · Текучесть · Найм · Минимум).

Куда доходит скрытие:

| Место | Реагирует |
|---|---|
| Строки таблиц OnePager, 6 hero-KPI, текст сводки (`worstMetrics`/`bestMetrics`) | да |
| Столбцы сводной таблицы подразделений и KPI-карточки блока (`mets` в `_block.js`) | да |
| Инсайты блока (`TPINSIGHTS.build`, `html`) | да |
| Пункт блока в навигации и блок на OnePager при полностью скрытом наборе | да |
| Графики в правой панели детального листа | **нет** |

Графики не трогаем сознательно: они рисуют смысл блока (баланс, воронка, водопад,
разбивки), а не список метрик, и «выключить серию» в них означало бы ломать
композицию, а не убирать строку.

Две страховки от битого состояния: `blockMain()` выбирает главную метрику только
среди видимых (от неё зависит колонка «К базе»), а `render()` в самом начале уводит
на OnePager, если активная вкладка — скрытый или несуществующий блок. Это одна точка
и на ссылку `?tab=tgrowth&hide=…`, и на отключение метрик в модалке.

## Правила визуализации

0. **Ось значений ВСЕГДА от нуля.** Никогда не урезаем: диапазон 6,1–6,9% на урезанной
   оси выглядит обвалом, хотя колебание меньше процентного пункта — это искажение данных.
   В `draw.js` нижняя граница — константный ноль, `niceMax()` считает только верх, поэтому
   урезанную ось там нельзя получить даже случайно.
1. **Ось Y не рисуем вообще**, если значения подписаны у точек и баров (а это режим по
   умолчанию). Ни линии оси, ни засечек, ни сетки — подпись уже несёт всё число,
   остальное лишние чернила. Остаётся только базовая линия нуля.
2. **Два бара рядом за один период не рисуем.** Динамика не читается, сравнивать тяжело.
   Пары «пришли / ушли» — `chart('diverge')` (вверх/вниз от общего нуля, **одна шкала на
   оба плеча**, иначе бары визуально несравнимы), разные метрики — `chart('panels')`
   (панели друг под другом с общей осью X, у каждой своя шкала от нуля).
3. **Подпись значения над каждой точкой и баром** — борт бизнесовый, значение читается
   без наведения.
4. **Разбивка по атрибутам — таблица, а не бар-чарт.** Причины увольнений, грейды, стаж,
   штат/не штат идут через `TPUI.barTable`: настоящая `<table>`, полоса **внутри ячейки**
   и растёт **от левого края** (общий старт у всех строк). Кольца и горизонтальные
   бар-чарты для этого не применяем: доля читается по числу точнее, чем по углу сектора,
   а таблица заодно даёт абсолютные значения и долю в процентах.
5. **Граница календарного года** — вертикальный пунктир ровно на высоту области
   построения. Не короче и не длиннее.
3. **Единая гамма потоков:** прибыло — `C_IN` (зелёный), убыло — `C_OUT` (красный).
   Одинаково в балансе, найме-оттоке и T-росте; приглушённых дублей не заводим.
4. **Заголовок графика слева, легенда справа** — не накладываются друг на друга.

6. **Подсказка собирается конструктором `TPDRAW.tipHtml`, а не склейкой тегов.**
   На вход — заголовок и массив строк `{name, val, color, dash}`, на выход — разметка
   `t-h` / `t-r` / `t-v`: маркер, подпись, значение с выравниванием по правому краю.
   Руками `<b>`-цепочки больше не пишем: строки в подсказках разных экранов расходились
   по кеглю, порядку и выравниванию. **`esc()` внутри объектов подсказок вызывать нельзя** —
   конструктор экранирует сам, иначе амперсанды экранируются дважды.
7. **Подпись значения поверх графика рисуется с halo** (опция `halo` в `txt()`): сначала
   белая обводка `stroke-width="3.2"`, потом заливка. `paint-order` не используем —
   в Safari он даёт двойной текст.
8. **Единая геометрия графиков.** Все виды берут одни константы из `draw.js`:
   `AXIS_H`, `HEAD_GAP`, `PAD_X`, `LBL_ROOM`, `VAL_SZ`/`VAL_W`/`VAL_DY`/`VAL_ASC`, `DRAW_MS`.
   Область построения — всегда `x0=PAD_X`, `plotW=w-PAD_X*2`; ни один вид не липнет
   к краю. Подписи значений задаются через `valOpt()`, а не набором опций на месте.
9. **Серии и легенда.** Состав серий графика описан в `SERIES` (`line`, `diverge`,
   `waterfall`, `bars`), каждая марка несёт **отдельный атрибут** `data-s="<id>"`.
   Идентификатор серии в класс не дописываем: `smoke.js` сверяет литералы
   `class="bar up"` / `class="bar dn"` посимвольно. Легенда рисует группы `.lg[data-sid]`
   с прозрачной ловушкой `.hit`: наведение подсвечивает серию (`highlight`), клик и
   Enter/Space выключают её (`toggleSeries`). Выключенная серия уходит из шкалы и из
   подсказки, а дивергент на одном плече перестраивается: ноль уезжает вниз (остался
   найм) или наверх (остался отток). Последнюю включённую серию выключить нельзя,
   `waterfall` заблокирован через `LOCKED` — композиция держится на всех типах столбцов.
   Выключение помнится в пределах сессии (карта `_off`, ключ `kind|title|cid`,
   не чистится в `reset()`) и сбрасывается перезагрузкой страницы.
10. **Шкала токенов в `:root`.** Расстояния — `--s1…--s10` (2/4/6/8/10/12/14/16/20/24),
   кегли — семь ролей `--fs-micro` 9,5 / `--fs-cap` 10,5 / `--fs-note` 11,5 /
   `--fs-body` 12,5 / `--fs-lead` 13,5 / `--fs-head` 16 / `--fs-hero` 24,
   радиусы — `--r1…--r5` (3/6/9/12/16) плюс `--r-pill` для чипов. Новые значения
   отступов, кеглей и радиусов берём из шкал, а не «на глаз».
11. **Цвета живут только в `draw.js` и `:root`.** В `screens/*.js` литеральных
   `#rrggbb` быть не должно — контроль `grep -rn "#[0-9a-f]\{6\}" screens/*.js`,
   он же отдельной проверкой в `smoke.js`.

Остальное из списка «НЕЛЬЗЯ ЛОМАТЬ» в `PROGRESS.md`: ключи метрик, поля state,
названия CSS-токенов.

## Экраны

- **OnePager** (`screens/onepager.js`) — hero-KPI из `HERO`, затем блоки метрик:
  значение / MoM / YoY / спарклайн / сравнение с базой. Клик по строке раскрывает
  линию «метрика + база + KPI-пороги» прямо внутри таблицы (`openRows`). Полный состав —
  6 карточек и 19 метрик в 7 блоках; фактический зависит от набора, выбранного
  пользователем в модалке (см. «Третье правило»).
- **Детальная вкладка** (`SC.renderBlock` в `screens/_block.js`, один каркас на все
  7 блоков) — заголовок блока со ссылкой на детальный дашборд справа → инсайт → KPI
  блока → `.split`: слева сводная таблица подразделений (ИТОГО первой строкой, sticky
  под шапкой; уровень −1, каретка раскрывает −2 через `expanded`), справа содержимое
  блока с под-вкладками. Клик по строке таблицы фильтрует правую панель (`selNode`).
  Тулбар появляется только при временном корне (`drillRoot`).
  **Высота `.split` — экран под шапкой** (`calc(100vh - var(--head-h) - 28px)`, минимум
  520px, только при ширине от 901px). Это не косметика: от неё зависит и число видимых
  строк в таблице подразделений, и возможность прокруткой убрать заголовок, инсайт и
  KPI-карточки, оставив в кадре только таблицу и график. Ограничивать высоту константой
  снова — значит вернуть обе проблемы. Графики с `fill:true` берут высоту из контейнера
  (`remeasure`), поэтому растягиваются сами и правки в экранах не требуют.
- Под-вкладки объявляются в самом файле экрана (`subTabs`), содержимое — в его `view()`.
  У `structure`, `tgrowth` и `monitor` по одной под-вкладке, поэтому переключатель
  не рисуется. `smoke.js` берёт список под-вкладок оттуда же, поэтому тест не может
  отстать от кода. `renderBlock` сверяет `S.subTab` со списком блока и откатывается
  на `defaultSub`: иначе старая ссылка `?sub=speed` собрала бы экран наполовину.

Под-вкладки после правок 01.08.2026 (итерация 20):

| Блок | Под-вкладки |
|---|---|
| `structure` | Разбивки по атрибутам (3 таблицы на одном экране) |
| `movement` | Баланс за период (динамика численности + водопад) · Найм, отток и переводы (два дивергента) |
| `turnover` | Отток и текучесть (3 панели) · Причины увольнений (таблица) |
| `hiring` | Вакансии и срок закрытия (3 панели) · Воронка |
| `tgrowth` | Конверсия и решения (линия + дивергент) |
| `monitor` | Динамика |
| `office` | Календарь (сетка + дни недели таблицей) · Рейтинг офисов · Динамика |

**Принцип этой раскладки:** одна под-вкладка = один вопрос целиком, а не один
график. После итерации 19 рабочая зона занимает экран, и на неё влезает пара
«что получилось + из чего сложилось»: численность и водопад, конверсия T-роста
и решения, из которых она считается. Обратная сторона — под-вкладку с одиноким
графиком заводить незачем: срок закрытия вакансии стал третьей панелью к объёму
вакансий, динамика численности переехала к водопаду в «Движение персонала».

Два графика в одной панели не требуют нового кода: `.split-r .panel-b` —
flex-колонка с `gap`, а `.svgchart.fill` берёт `flex:1`, поэтому оба делят высоту
пополам и перемеряются под контейнер сами.

### Посещаемость офисов

Три вкладки вокруг одной метрики `office_att`:

- **Календарь** — `chart('calendar')`: месячная сетка Пн–Вс за последний месяц окна,
  в ячейке день и процент, шкала **монохромная синяя, не светофор** (5% в субботу —
  не «плохо», офис в выходной закономерно пуст), выходные выведены из шкалы отдельным
  светлым тоном. Под сеткой — дни недели через `barTable`: день недели это атрибут,
  а разбивки по атрибутам в проекте таблицей.
- **Рейтинг офисов** — `barTable` с `share:false`: доля одного процента в сумме
  процентов ничего не значит, поэтому колонки «Доля» и строки ИТОГО там нет.
- **Динамика** — прежняя линия с базой сравнения.

Данные: `D.attDays(lp,[mIdx])`, `D.attByDow(lp)`, `D.officeRank(lp)`. Инвариант,
который держит эти экраны непротиворечивыми: **среднее по будням календаря равно
`office_att` за тот же месяц**, а сумма людей по офисам равна `hc_total` отбора.
На это есть проверки в `smoke.js` — если генератор поправят, они это поймают.

## Движок инсайтов

`TPINSIGHTS.build` (`insights.js`) — не текстовый шаблон, а отбор фактов по трём порогам
из шапки файла: `INS_GAP=10` (% отклонения от базы или KPI), `INS_TREND=3` (месяцев
направленного тренда), `INS_CONC=50` (% отклонения, приходящегося на одно подразделение).
Факты сортируются по severity 1–3, она же красит плашку (`sev-high/mid/good`). Нет фактов
выше порога — вместо блока одна строка `.no-insight`. Правило: **нет инсайта — нет
блока**, никакой воды.

На OnePager другой, более простой генератор (`lead` / `bullets` поверх
`worstMetrics` / `bestMetrics` в `screens/onepager.js`).

## Что помнить при правках

- Вся разметка — конкатенация строк с ручным `esc()` (`TPUI.esc`). Пользовательских
  данных нет, но имена узлов проходят через `esc` — так и оставлять.
- Экраны **возвращают строку**, а не пишут в DOM. Весь рендер — одна вставка в `#view`
  в `app.js`, после неё `TPDRAW.remeasure()`. Отдельного монтирования графиков нет,
  поэтому и утечки инстансов больше нет: `TPDRAW.reset()` чистит реестр спек.
- В `app.js` **один** `click`-слушатель на `document` с порядком проверок от частного к
  общему: `.urow` ловит клики очень широко, поэтому идёт последним, а `data-exp` и
  `data-drill` со `stopPropagation` — раньше него. Отдельная ловушка: кнопки пресетов
  метрик тоже имеют класс `.opt`, но без `data-f`, поэтому `data-mpreset` и `data-mtoggle`
  проверяются **до** общей ветки `.opt` — иначе она пишет в `DRAFT[undefined]`.
- `render()` — единственная точка входа, обёртки `_renderCore` больше нет:
  `enhanceA11y()` и `navOpen(false)` вызываются внутри неё.
- Одна итерация — одна область. `data.js` и экраны не править в одном заходе; правки
  точечные, файл целиком не переписывать.
- Ось значений и подписи — не вкусовщина, а правила из раздела выше. Прежде чем менять
  геометрию в `draw.js`, прогнать `node smoke.js`: там 9 проверок именно на это.

## Расхождения документации и кода (на 31.07.2026, после снятия ECharts)

- Итерации 8–12 задним числом не восстановлены — в `PROGRESS.md` про них одна отметка.
  Что именно менялось, видно только по секциям в `styles.css`.
- UI для смены `mainMetric` нет: `blockMain()` отдаёт первую сравнимую метрику среди
  выбранных в блоке. Поле в state есть и работает, но переключить его пользователь
  не может — он влияет на выбор только косвенно, отключая метрики.
- Воронка найма считается от открытых вакансий коэффициентами 0,63 / 0,49 / 0,41 — это
  выдумка макета, реального источника под ней нет. В UI помечено сноской.
- **Подневная посещаемость и офисы — тоже выдумка макета** (`attDays`, `attByDow`,
  `officeRank`, справочник `OFFICES` из 6 офисов). Ни подневных отметок, ни привязки
  сотрудников к офисам в модели нет: календарь разбрасывает будни вокруг `office_att`
  месяца с нормировкой среднего, выходным даёт 5–14% от него, доли по офисам берёт
  из seed. В UI обе вкладки помечены сноской. Сходится с фактами ровно в двух точках,
  и они проверяются тестом: среднее по будням = `office_att` месяца, сумма людей
  по офисам = `hc_total` отбора. При подключении к хранилищу это заменяется первым.
- `regret` по-прежнему генерируется от `anchor` 5,1% и не выводится из причин увольнений,
  хотя `EXIT_REASONS` знает, какие причины нежелательные. Поэтому сумма regret-причин в
  таблице и значение метрики `regret` между собой не сходятся.
- «Прочие изменения» в водопаде баланса закрывают невязку: численность генерируется
  независимо от потоков найма и оттока.

## Решения и договорённости

**30.07.2026, итерация 13** (правки по итогам первой приёмки):

- Сравнимость метрик разделена по типу: относительные сравниваем, счётные — нет.
  Причина от заказчика: «зачем сравнивать численность с общей численностью HQ IT»,
  «срок закрытия вакансии сравнивать можно, количество закрытых — бред».
- Два бара рядом за один период запрещены как класс визуализации, а не точечно.
- «Текучесть МТД и накопительная ИТД» реализованы существующими метриками
  `turnover_m` (месячная) и `turnover_y` — тогда скользящей за 12 месяцев.
  *Отменено 31.07.2026: `turnover_y` стала накопительной с начала года.*
- В «Динамике оттока» убрана серия regrettable: она была вторым баром рядом. Если нужна —
  вернём отдельной панелью или линией поверх баров.
- Причины увольнений: сделаны горизонтальными барами от большего к меньшему.
  *Отменено 31.07.2026: нужна была именно таблица, переделано на `TPUI.barTable`.*
- Панели линию базы не рисуют: база показана в KPI-карточках над таблицей.

**31.07.2026:** снапшоты `*.orig.*` (состояние файлов до итерации 13) удалены по просьбе
пользователя — дублировали рабочие файлы и ни одним скриптом не читались. Дифа с
состоянием «до правок» больше нет.

**31.07.2026, итерация 14** — снятие библиотек и вторая приёмка:

- **ECharts убран полностью.** Причина: конечный инструмент заказчика не поддерживает
  библиотеки. Всё рисование переехало в `draw.js` на голом SVG. Сдача — одним файлом
  через `node build.js`.
- **Разбивка по слоям и экранам** вместо четырёх больших файлов. Причина прямая: у
  пользователя жёсткие суточные лимиты, и правка одного экрана не должна означать чтение
  40 КБ `app.js`. Экранам запрещено писать разметку самим — иначе общие элементы
  расходятся, а это ровно та цена, из-за которой разбивку обычно не делают.
- **Ось значений всегда от нуля**, ось Y при подписанных значениях не рисуется. Триггер —
  недоработчики: диапазон 6,1–6,9% на урезанной оси читался как обвал.
- **Текучесть пересобрана.** `turnover_m` стала производной `отток / среднесписочная ×
  100%`, добавлена метрика `hc_avg`. `turnover_y` — не скользящая, а **накопительная**:
  сумма месячных процентов с обнулением каждый январь. Для этого генератор считает
  18 месяцев (6 до окна), иначе YTD июля 2025 не с чего начинать.
  Формулу «среднесписочная / отток» из ТЗ прочитали как обратную: буквальная давала
  бы 21 вместо 4,7%.
- **Ставки генератора пересчитаны.** Когда `turnover_m` стала производной, вылез
  настоящий масштаб `attrition`: текучесть выходила 4–8% в месяц (38–58% годовых).
  Счётные метрики теперь считаются от месячной ставки с сезонностью и стохастическим
  округлением; месячная текучесть 0,5–3,1%, годовой прогон ~21%.
- **Разбивки — таблицами.** Кольца по грейдам / стажу / штату и горизонтальные бары
  причин увольнений заменены на `TPUI.barTable` с полосой в ячейке по левому краю.
  Четыре под-вкладки структуры свернуты в одну «Разбивки по атрибутам»: три таблицы на
  одном экране, перекликивать больше нечего.
- **Отток и текучесть разведены** на три панели: факт в людях, месячная текучесть,
  накопительная. Одна ось X, у каждой панели своя шкала от нуля.
- Починены два дефекта ECharts-версии: подписи нижних баров дивергентного чарта налезали
  на ось X (не было отведено место под них при общей шкале плеч) и граница года рисовалась
  не на всю высоту области построения.
- Попутно найден и исправлен баг `mixParts`: функция получала массив путей, а обращалась
  к элементам как к узлам (`n.grades`), поэтому все грейды выходили равными, а «штат / не
  штат» целиком падал в «не штат».

**01.08.2026, итерация 15** — выбор состава метрик:

- В модалку добавлена пятая группа «Метрики в отчёте»: строка пресетов
  (Всё · Текучесть · Найм · Минимум) плюс полный список по блокам с чекбоксами.
  Пресет только проставляет галочки, дальше набор донастраивается руками, и подсветка
  пресета гаснет. Оба варианта из обсуждения — готовые наборы и ручное отключение —
  закрыты одним механизмом, а не двумя.
- В state лежат **скрытые** метрики (`hiddenMetrics`), а не показанные, чтобы новая
  метрика появлялась у всех сама. Набор шарится ссылкой (`hide=`).
- Обязательные метрики — активная и общая численность. Общую залокали не по просьбе,
  а по устройству отчёта: на ней держится подпись «N чел» в каждой строке сводной
  таблицы и знаменатель долей.
- Границу провели по «списку метрик против визуализации»: скрытие доходит до строк
  OnePager, столбцов сводной таблицы, KPI-карточек и инсайтов, но не до графиков
  в правой панели детального листа. Убрать серию из водопада или воронки — это
  сломать композицию, а не убрать строку.
- Блок с полностью отключённым набором исчезает и из OnePager, и из левой навигации;
  `render()` уводит на OnePager, если открыта вкладка такого блока. Заодно перестал
  падать вход по ссылке с мусорным `?tab=`.

**01.08.2026, итерация 19** — два цвета в светофоре и рабочая зона во весь экран:

- Жёлтый убран из светофора: «на уровне ±5%» и «зона риска» по KPI стали серыми
  и в пилюлях, и в спарк-барах, и в рамке строки OnePager. Формулировка заказчика:
  «уже надо к чему-то присматриваться» — три сигнальных цвета заставляли выбирать,
  к какому из них. Осталось зелёный / красный / серый, где серый значит «повода
  вмешиваться нет». Легенда и справка сжались с четырёх пунктов до трёх.
- Тег `KPI` перекрашен из жёлтого в синий. Он не оценка, а метка «у метрики есть
  цель»; в жёлтом он оставался последним ложным сигналом на OnePager.
- Состояние `warn` в логике сохранено намеренно: подписи «на уровне» и «зона риска»
  различаются, а цвет у них теперь один. Стирать `warn` из `compareState` значило бы
  потерять текст, который пользователю полезен.
- `.split` растянут на экран под шапкой вместо константных 620px, нижний отступ
  контента уменьшен 64 → 24px. Запрос был двойной: на реальных данных подразделений
  больше, чем в моке, и в таблицу должно влезать больше строк; плюс прокруткой нужно
  уметь убрать заголовок, инсайт и KPI, оставив только таблицу и график. Оба следствия
  дались одной правкой: длина страницы теперь ровно на высоту верхних блоков больше
  экрана, поэтому прокрутка доводит рабочую зону точно под шапку.

**01.08.2026, итерация 20** — перекомпоновка под-вкладок и блок офисов:

- Раскладка пересобрана по принципу «одна под-вкладка = один вопрос целиком».
  Прямое следствие итерации 19: на экранную рабочую зону влезает пара графиков,
  поэтому «что получилось» и «из чего сложилось» больше не разнесены по вкладкам.
  Динамика численности переехала из структуры в «Движение персонала» к водопаду,
  срок закрытия вакансии стал третьей панелью к объёму вакансий, T-рост собран
  в одну вкладку (конверсия + решения, из которых она считается).
- Переводы в/из показаны вторым дивергентом на вкладке потоков, в той же гамме
  `C_IN`/`C_OUT`, что найм и отток. Отдельный «приглушённый» цвет для внутренних
  переводов не заведён намеренно: водопад на соседней вкладке красит те же переводы
  зелёным и красным, и расхождение читалось бы как разные сущности.
- Блок посещаемости офисов развёрнут с одной вкладки до трёх: календарь с днями
  недели, рейтинг офисов, динамика. Запрос был «посмотреть офис календарём» —
  под это в `draw.js` добавлен вид `calendar`, а в `data.js` подневный генератор.
- Шкала календаря монохромная: посещаемость выходного дня не оценка. Светофор там
  красил бы субботу красным и врал.
- Подневные данные и офисы сгенерированы — в UI сноска, в «Расхождениях» запись.

**02.08.2026, итерация 21** — чистка интерфейсного шума:

- Пустое состояние фильтров убрано совсем: нет разрезов — нет и плашки «Разрезы
  не выбраны». Она занимала строку в шапке и ничего не сообщала. Класс `.chip.empty`
  удалён из CSS вместе с ней.
- Стрелки `↑`/`↓` в дельтах заменены знаками `+`/`−`, нулевое изменение — просто `0`.
  Правка одной функции `deltaChip` покрыла и KPI-карточки, и OnePager: обе рисуются
  через неё.
- `↗` у кнопки «Детальный дашборд» заменён на иконку `TPUI.icoExt()` — инлайн-SVG.
  Символ брался из шрифта и в разных начертаниях выходил то мелким, то съехавшим.
  Побочный эффект, о который споткнулся тест: подсчёт графиков в `smoke.js` считал
  теги `<svg>` и стал захватывать иконку. Теперь считаются `.svgchart`.
- Кнопка перехода стала ниже (`padding:6px 12px`), у `.ph-row` снят нижний отступ:
  высокая кнопка растягивала строку заголовка, и между заголовком листа и его
  подзаголовком набегал лишний воздух.
  Чтобы выдумка не разъезжалась с остальным отчётом, зафиксированы два инварианта
  (среднее по будням = `office_att`, сумма людей по офисам = `hc_total`), и на оба
  есть проверки в `smoke.js`.

**02.08.2026 — удалён `charts.js`:**

- Старая реализация графиков на ECharts (374 строки, 24,8 КБ) удалена из папки.
  Она лежала мёртвым грузом с итерации 14: неймспейс `window.TPCHARTS` не читал
  никто, файл не был подключён ни к `index.html`, ни к `smoke.js`, ни к сборке.
  Держали его на случай, если понадобится сверяться со старым поведением; за
  семь итераций такой нужды не возникло, а история файла есть в git.
- Упоминания остались в исторических записях `PROGRESS.md` — журнал итераций
  задним числом не переписываем. Из `SPEC.md` упоминание ушло вместе с его
  переписыванием (см. запись ниже).

**02.08.2026 — удалены мёртвые CSS-классы:**

- Из `styles.css` убраны 10 неиспользуемых классов (19 правил): `.grid2`, `.seg`,
  `.bar-cell/.bar-bg/.bar-fg`, `.caret`, `.val-pos/.val-neg/.val-neu`, `.chart-main`.
  Последний — контейнер ECharts-эпохи; графики давно приходят в `.svgchart`.
- Проверялись не грепом по подстроке, а разбором того, что код реально выдаёт
  в разметку: все литералы `class="…"` плюс `classList.add/remove/toggle` из
  `index.html` и всех восьми модулей JS. Грепа бы не хватило — `seg` встречается
  в `seg:'IT'` внутри `data.js`, а `caret` живёт в `.caret-btn`, `.caret-spacer`,
  `.ai-caret` и `.pd-caret`, которые все используются.
- Отдельная ловушка: семейство `.m-name.bar-good/.bar-bad/.bar-warn/.bar-neutral`
  в разметке буквально не встречается — класс склеивается в `screens/onepager.js`
  как `'m-name bar-'+st`. Оно живое и оставлено.
- Правила лежали не только отдельными строками, но и вкраплениями в общие
  медиа-блоки (`max-width:1100/900/480`, `@media print`) — оттуда вынуты точечно,
  соседние селекторы не тронуты.
- После правки: `node smoke.js` — 0 FAIL, `node build.js` — сборка на 1 428 символов
  короче.

**02.08.2026 — `SPEC.md` переписан, мёртвые поля state удалены:**

- Из `DEFAULT_STATE` убраны `level`, `viz` и `openMetric`. Это не сокращение
  контракта, а вынос остатков: механизмов, которым поля принадлежали, в коде нет.
  Уровень −2 раскрывается каретками (`expanded` в `app.js`), переключателя
  визуализации нет с итерации 14, раскрытые строки OnePager живут в `openRows`.
  Ни одно из трёх не читалось нигде — проверено поиском `S.` / `DRAFT.` / `st.`
  по всем модулям.
- `period` **оставлен намеренно.** Он тоже не читается, но это не остаток
  удалённого механизма, а зеркало живой константы `PERIOD_LABEL` и естественный
  задел под выбор периода. Убирать его — отдельное продуктовое решение, а не чистка.
- `SPEC.md` переписан целиком, а не построчно. Он разошёлся с кодом ещё на снятии
  ECharts: перечислял `charts.js` в архитектуре, обещал «ECharts из CDN», описывал
  контракт state с полями `level` и `viz`, знал 18 метрик вместо 19 и приписывал
  метрике поля `drillUrl` и `aiTemplate` (первое принадлежит блоку, второго нет
  вовсе). Наполовину обновлённый SPEC хуже честно устаревшего.
- В новый SPEC добавлено то, чего в нём не было: третье правило (состав метрик),
  слой `mascot.js`, правила про halo, `tipHtml`, единую геометрию, серии и легенду,
  запрет литеральных цветов в экранах, шкалы токенов, два цвета светофора и знак
  вместо стрелки.
- Экспортную поверхность неймспейсов не трогали. Формально «не используется
  снаружи» 20 имён `TPDATA` и 19 `TPDRAW`, но это не мёртвый код: `redraw`,
  `PALETTE`, `C_GREEN` работают внутри `draw.js`, `descendantsOf` — внутри
  `data.js`. Сокращать экспорт — отдельная затея с риском и без выигрыша.
